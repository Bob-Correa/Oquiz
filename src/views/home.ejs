<%- include('partials/head') %>

        <main class="container py-4">
            <!-- Welcome -->
            <div class="text-center">
                <h2 class="h1 my-4">Bienvenue sur O'Quiz</h2>
                <p class="lead">
                    Bienvenue sur O'Quiz, la plateforme qui allie savoir et divertissement !
         Plongez dans un monde de quiz captivants où chaque catégorie vous promet un voyage unique.
          Que vous soyez curieux de découvrir des anecdotes sur des animaux célèbres, ou gourmand d'apprendre plus sur l'univers du chocolat,
           il y en a pour tous les goûts. Choisissez votre niveau, relevez le défi, et amusez-vous en élargissant vos connaissances !.</p>
                </p>
            </div>

            <!-- Cards -->
            <div class="row mb-3 text-center flew-wrap">
                <!-- One card -->

                <% if (locals.quizzes?.length) { %>

                    <% for( let i = 0; i < quizzes.length; i++ ) { %>

                        <div class="col-4">
                            <div class="card mb-4">
                                <div class="card-header">
                                    <h3 class="h2"><%= quizzes[i].title %></h3>
                                </div>
                                <div
                                    class="card-body d-flex flex-column justify-content-between"
                                >
                                    <h4 class="h3"><%= quizzes[i].description %></h4>
                                    <p>par <%= quizzes[i].author.fullname %></p>
                                    <ul class="list-unstyled mb-4 p-0">
                                        <% for( let j = 0; j < quizzes[i].tags.length; j++ ) { %>
                                            <li class="badge bg-primary"><%= quizzes[i].tags[j].name %></li>
                                        <% } %>
                                    </ul>
                                    <a
                                        href="/quiz/<%= quizzes[i].id %>"
                                        class="btn btn-lg btn-primary text-white"
                                        >Jouer</a
                                    >
                                </div>
                            </div>
                        </div>

                    <% } %>

                    <% for( const quiz of quizzes ) { %>

                        <div class="col-4">
                            <div class="card mb-4">
                                <div class="card-header">
                                    <h3 class="h2"><%= quiz.title %></h3>
                                </div>
                                <div
                                    class="card-body d-flex flex-column justify-content-between"
                                >
                                    <h4 class="h3"><%= quiz.description %></h4>
                                    <p>par <%= quiz.author.fullname %></p>
                                    <ul class="list-unstyled mb-4 p-0">
                                        <% for( const tag of quiz.tags ) { %>
                                            <li class="badge bg-primary"><%= tag.name %></li>
                                        <% } %>
                                    </ul>
                                    <a
                                        href="/quiz/<%= quiz.id %>"
                                        class="btn btn-lg btn-primary text-white"
                                        >Jouer</a
                                    >
                                </div>
                            </div>
                        </div>

                    <% } %>


                    <% quizzes.forEach(quiz => { %>

                        <div class="col-4">
                            <div class="card mb-4">
                                <div class="card-header">
                                    <h3 class="h2"><%= quiz.title %></h3>
                                </div>
                                <div
                                    class="card-body d-flex flex-column justify-content-between"
                                >
                                    <h4 class="h3"><%= quiz.description %></h4>
                                    <p>par <%= quiz.author.fullname %></p>
                                    <ul class="list-unstyled mb-4 p-0">
                                        <% for( const tag of quiz.tags ) { %>
                                            <li class="badge bg-primary"><%= tag.name %></li>
                                        <% } %>
                                    </ul>
                                    <a
                                        href="/quiz/<%= quiz.id %>"
                                        class="btn btn-lg btn-primary text-white"
                                        >Jouer</a
                                    >
                                </div>
                            </div>
                        </div>

                    <% }) %>

                <% } else  { %>
                    <p>Nous n'avons rien à afficher pour le moment</p>
                <% }  %>



            </div>
        </main>

<%- include('partials/foot') %>
